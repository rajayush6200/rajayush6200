# Ayush Raj — GitHub Profile Repository

> This is the special **`rajayush6200/rajayush6200`** profile repository.
> The `README.md` in this repo is displayed on [github.com/rajayush6200](https://github.com/rajayush6200).

## 📁 Structure

```
rajayush6200/
├── README.md        ← Main profile README (rendered on profile)
├── SETUP.md         ← This file
└── assets/          ← Static assets (manually managed)
```

## ℹ️ Design Decisions

This repository uses **no GitHub Actions** by design. All content in the README is either:

- **Static** — written manually and committed directly
- **Live API widgets** — rendered at view-time via external badge/card services (shields.io, github-readme-stats, etc.) that require no bot commits

This ensures **only your GitHub account** ever appears as a contributor.

## 🎨 Personalization Checklist

- [ ] Replace `rajayush6200` in LeetCode badge with your actual LeetCode handle
- [ ] Add real certification URLs to badge links
- [ ] Update LinkedIn/Twitter URLs if different from your username
- [ ] Update portfolio URL in social badges
- [ ] Verify WakaTime username if you use the WakaTime stats card

## ✅ No GitHub Secrets Required

All stats widgets (GitHub stats, streak, top languages, LeetCode, activity graph, trophies) are read-only, live-rendered services. No tokens, no secrets, no automation needed.
